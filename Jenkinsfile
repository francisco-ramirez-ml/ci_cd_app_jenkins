pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                echo 'Checkout step'
            }
        }
        stage('Terraform init') {
            steps {
                echo 'terraform init'
            }
        }
        stage('Terraform apply') {
            steps {
                echo 'terraform apply'
        }
    }
}
